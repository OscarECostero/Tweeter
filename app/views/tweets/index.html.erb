<section class="section">
  <div class="container">
    <div class="columns">
      <%= render 'feed' %>
      <%= render 'who_to_follow' %>
      <%= render 'trends' %>

    </div>
  </div>
</section>

